# permissions for end users to view templates.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: template-viewer-role
rules:
- apiGroups:
  - experiments.cnrm.cloud.google.com
  resources:
  - templates
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - experiments.cnrm.cloud.google.com
  resources:
  - templates/status
  verbs:
  - get
